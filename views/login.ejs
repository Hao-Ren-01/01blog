<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('head',{});%>
    <style>
      .layui-form{
        margin: 50px auto;
        width: 500px;
      }
      .layui-form-label{
        font-weight: bold;
        color:rgb(230, 230, 230);
        width: 50px;
      }
      .btn{
        margin: 0px auto;
      }
      .layui-btn{
        background-color: #393d49;
      }
      .layui-btn a{
        color:rgb(230, 230, 230);
      }
    </style>
</head>
<body>
  <%- include('header',{ userName }) %>
    <div class="layui-container">
        <form class="layui-form" action="/users/login" method="POST" >
            <div class="layui-form-item">
              <label class="layui-form-label">账号：</label>
              <div class="layui-input-block">
                <input type="text" name="username"   placeholder="请输入用户名" class="layui-input">
              </div>
            </div>
            <div class="layui-form-item">
              <label class="layui-form-label">密码：</label>
              <div class="layui-input-block">
                <input type="password" name="password"  placeholder="请输入" class="layui-input">
              </div>
            </div>
            <div style="width: 100px;" class="btn">
              <button type="submit" class="layui-btn layui-btn-fluid">登录</button>
            </div>
            <div>
              没有账号，去<a href="/regist">注册</a>
            </div>
        </form>
    </div>
    
</body>
</html>